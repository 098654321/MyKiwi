## TODO

- 增量布线算法调整：

  - 目前的迭代在case21当中无法收敛，需要检查无法收敛的原因，调整收敛条件

- 修改要点

  - 原来读入一个mode配置，进行另一个mode布线的功能不能删。现在一次性读入两个mode配置然后一起布线的功能需要单独开一个模块，作为一个新的命令行可选功能

  - try_all_modes 是一次性读入两个mode然后一起布线的标记，与该标记相关的函数都需要修改。

--- 

- 把 vpr 针对 high-fanout 的优化加上

- 一对多的 net 的 path_in_order() 函数需要重新实现

- 加一个功能：如果吧其它 mode 的布线结果加进来，但是布线失败了，就把与失败的线冲突的、已经加载的线移除重布

- GUI 还没有适配增量布线**
